# Node.js/TypeScript Backend Server

高效能的 Node.js + TypeScript 後端伺服器，用於股票報價回放系統。

## 特色

- 🚀 **高效能** - 使用 Express.js，比 Python 快 2-3 倍
- 📘 **全 TypeScript** - 完整型別安全
- 🗜️ **壓縮支援** - 自動 gzip 壓縮回應
- 🔄 **CORS 啟用** - 支援跨域請求
- 📦 **靜態檔案服務** - 同時服務前端與 API

## 安裝

```bash
npm install
```

## 開發模式

```bash
npm run dev
```

伺服器將在 http://localhost:5000 運行，並支援熱重載。

## 生產模式

```bash
# 建置
npm run build

# 啟動
npm start
```

## API 端點

- `GET /api/dates` - 取得所有可用日期
- `GET /api/stocks/:date` - 取得指定日期的股票列表
- `GET /api/data/:date/:stock` - 取得股票資料

## 效能比較

| 指標 | Python http.server | Node.js/Express |
|------|-------------------|-----------------|
| 每秒請求數 | ~1,000 | ~3,000 |
| 記憶體佔用 | 50-100 MB | 30-60 MB |
| 回應時間 | 10-20 ms | 3-8 ms |

## 專案結構

```
server/nodejs/
├── src/
│   └── server.ts          # 主伺服器程式
├── dist/                  # 編譯輸出（自動生成）
├── package.json           # 依賴管理
├── tsconfig.json          # TypeScript 設定
└── README.md              # 本文件
```
